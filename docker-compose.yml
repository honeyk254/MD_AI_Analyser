version: "3.8"

services:
  api:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./data:/app/data
    environment:
      - DEBUG=True
      - OUTPUT_DIR=/app/data/outputs
      - DATA_DIR=/app/data/inputs
